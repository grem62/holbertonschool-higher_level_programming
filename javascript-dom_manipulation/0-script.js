// Sélectionner l'élément d'en-tête par son sélecteur CSS
const headerElement = document.querySelector('header');

// Mettre à jour la couleur du texte en rouge (#FF0000)
headerElement.style.color = '#FF0000';
